// 错误代码定义类
export class ErrorEventsListenerCodes {
    // 成功（虽然在Java中未定义，但在TS中可作为参照对比）
    static SUCCESS = 0;
    // 初始化参数错误
    static INIT_PARAM_ERROR = -1;
    // 已经初始化过
    static INIT_ALREADY = -2;
}
